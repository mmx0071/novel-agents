# 复制为 .env（已被 gitignore）。
# 模型与 API Key 由 dsh 自己管（~/.dsh/settings.yaml 或 dsh Web 配置）。
# 本文件只给本机可选覆盖。

# 阅读台绑定（可选；默认 127.0.0.1:8765）
# NOVELX_BIND=127.0.0.1:8765

# dsh Web（可选；默认 127.0.0.1:3080）
# DSH_BIND=127.0.0.1:3080
# DSH_BIN=/path/to/dsh
# DSH_ROOT=/path/to/deepseek-harness

# 仓库根（在仓库外启动 dsh 时需要）
# NOVELX_ROOT=/path/to/novel-agents
