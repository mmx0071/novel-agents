# novelx_check 读取的相位开关。缺省视为 true。
version: 1

features:
  # setup_phase != ready 时拦写新章
  studio.enforce_setup_gate: true
  # 非 drafting_volume 时拦写新章
  studio.enforce_volume_phase: true
  # 跳章或前章缺口时拦写新章
  studio.enforce_chapter_order: true
